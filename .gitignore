# Audio files — keep tracks local, never commit them
sounds/*
!sounds/.gitkeep

# Aufnahmen aus oeuvre/-Kompositionen — bleiben lokal
recordings/*
!recordings/.gitkeep

# SuperCollider / macOS cruft
.DS_Store
*.scsyndef
.idea/*
presets/*
