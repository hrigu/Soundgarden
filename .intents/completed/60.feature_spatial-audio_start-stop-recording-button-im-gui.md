# 60 - Feature (Spatial-Audio): Start/Stop-Recording-Button im GUI

## 1. Why

### 1.1 Objective

Aktuell nimmt jede `oeuvre/`-Komposition (`dawn_chorus.scd`, `messiaen_birds.scd`) den
kompletten Stück-Ablauf über manuell hingeschriebenen `s.record(path:, duration:)`-Code am
Dateiende auf, mit dupliziertem Pfad-/Zeitstempel-Boilerplate. Der Nutzer möchte stattdessen
einen Start/Stop-Recording-Button im GUI-Kontrollpanel, das bei allen Spatial-Audio-Demos/
Kompositionen ohnehin schon offen ist.

### 1.2 Context

Nutzer-Entscheide (AskUserQuestion):
- Button lebt in `SpatialControlPanel`/`RoomControlPanel` (geteilte Infrastruktur), nicht nur
  in einzelnen Kompositionsskripten.
- Nur WAV vorerst — keine automatische m4a-Konvertierung (SuperCollider/libsndfile kann kein
  AAC/m4a schreiben, das bliebe ohnehin ein externer Konvertierungsschritt danach).

Depends On: Intent 46 (`presetsDir`/`scenesDir`-Konstruktor-Parameter-Muster, dem
`recordingsDir` gleichartig folgt).

## 2.0 What

```gherkin
Feature: Start/Stop-Recording-Button im GUI

  Scenario: Aufnahme starten
    Given ein offenes RoomControlPanel/SpatialControlPanel mit gesetztem recordingsDir
    When der Nutzer auf "Aufnahme starten" klickt
    Then beginnt der Server, den Output nach recordingsDir/<prefix>_<Zeitstempel>.wav
      aufzunehmen
    And der Button-Zustand wechselt auf "Aufnahme läuft"

  Scenario: Aufnahme stoppen
    Given eine laufende Aufnahme
    When der Nutzer auf "Aufnahme stoppen" klickt
    Then stoppt der Server die Aufnahme
    And der Button-Zustand wechselt zurück auf "Aufnahme starten"

  Scenario: Kein doppelter Start/Stop
    Given eine bereits laufende Aufnahme
    When start() erneut aufgerufen wird
    Then wird kein zweiter Server-Recording-Aufruf ausgelöst (gleicher Pfad zurückgegeben)
    And stop() ohne laufende Aufnahme ist ein stiller No-op
```

## 3.0 How

### 3.1 Implementation Context

- `classes/sg/spatial/RoomRecorder.sc` (neu): kapselt Dateiname-Erzeugung (`pathFor`, reine Logik,
  testbar) und Start/Stop (`server.record`/`server.stopRecording`), idempotent wie
  `SoundObject>>stop` (Intent 59-Folgefix).
- `classes/sg/gui/RoomParamsView.sc`: neuer optionaler "Recording"-Bereich (analog
  `installSceneControls`/`scenesDir`), nur gebaut wenn `recordingsDir` gesetzt ist. Button mit
  zwei Zuständen (`.states_([...])`, wie der Solo-Button in `SoundObjectControlsView`).
- `recordingsDir`-Parameter durchgereicht: `SpatialControlPanel` → `RoomControlPanel`, gleiches
  Muster wie `presetsDir`/`scenesDir`.
- `oeuvre/dawn/dawn_chorus.scd` und `oeuvre/messiaen_birds/messiaen_birds.scd`: manuellen
  `s.record(...)`-Block durch `recordingsDir:`-Parameter am `RoomControlPanel` ersetzen (löst
  auch die feste `duration:`-Vorabschätzung ab, die Aufnahme läuft jetzt bis zum manuellen Stop).
- Tests: `tests/sg/spatial/TestRoomRecorder.sc` (reine Logik: `pathFor`, Start/Stop-Zustandsübergänge
  mit Fake-Server, Idempotenz).

### 3.2 Tasks

- [x] 1.0 - RoomRecorder-Klasse
    - [x] 1.1 - TDD: Test für `RoomRecorder` (`pathFor`, Start/Stop-Zustand, Idempotenz)
    - [x] 1.2 - `RoomRecorder` implementieren, headless grün (Umbenennung von "Recorder" auf
          "RoomRecorder" -- Namenskollision mit SuperCollider-Kernklasse `Recorder`)
- [x] 2.0 - GUI-Integration
    - [x] 2.1 - `recordingsDir`-Parameter durch `RoomControlPanel`/`SpatialControlPanel`
          durchreichen
    - [x] 2.2 - `RoomParamsView`: Recording-Bereich mit Start/Stop-Button (+ Sicherheitsnetz:
          `SpatialControlPanel>>stop` stoppt eine noch laufende Aufnahme beim Schliessen)
    - [x] 2.3 - Hörtest/Sichttest durch den Nutzer: Button startet/stoppt echte Aufnahme,
          Datei landet in `recordings/`
- [x] 3.0 - Bestehende Kompositionen umstellen
    - [x] 3.1 - `dawn_chorus.scd`/`messiaen_birds.scd`: manuellen `s.record`-Block durch
          `recordingsDir:` am `RoomControlPanel` ersetzt (dawn_chorus.scd zusätzlich: alter
          duration-Vorabschätzungs-/Fallback-stopRecording-Code entfernt, ersetzt durch
          SpatialControlPanel-Sicherheitsnetz)
    - [x] 3.2 - Kurzer Sichttest durch den Nutzer: beide Skripte laufen weiterhin, Button
          funktioniert
- [x] 4.0 - Abschluss
    - [x] 4.1 - Headless-Testlauf grün
    - [x] 4.2 - Doku (CLAUDE.md) um `RoomRecorder` ergänzt
    - [x] 4.3 - Intent nach `completed/` verschieben
