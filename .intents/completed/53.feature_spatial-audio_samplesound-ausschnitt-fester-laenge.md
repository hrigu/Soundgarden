# 53 - Feature (Spatial-Audio): SampleSound spielt für `sample.scd` sinnvolle Ausschnitte

## 1. Why

### 1.1 Objective
`SampleSound` um sinnvolle Ausschnittswahl für `sample.scd` erweitern: kurze Samples sollen
vollständig gespielt werden, längere Samples automatisch auf einen relevanten Ausschnitt
gesetzt werden, statt oft in einem unergiebigen Bereich zu landen.

### 1.2 Context
Der erste Teil dieses Intents hat `startFrac` und `duration` als manuell konfigurierbare
Ausschnittsparameter eingeführt. Das reicht technisch, ist für `demos/sample.scd` aber zu grob:
bei heterogenen Percussion-Dateien werden kurze One-Shots unnötig abgeschnitten, während
längere Dateien mit einem festen Globalwert oft in einem Abschnitt mit wenig relevantem
Klangmaterial landen. Inzwischen enthält `demos/sample.scd` zudem mehrere zusammenhängende
Heuristiken für Ausschnitts- und Lautheitswahl direkt im Skript. Das funktioniert, macht das
Demo aber unnötig lang und bindet die Logik an genau diese eine Datei statt an eine klar
benannte, testbare Klasse.

### 1.3 Related To Intents
* 20 - `SampleSound` eingeführt (rhythmisches Sample-Playback).
* 43 - `editableParams`/Live-GUI-Bearbeitung für Sound-Parameter — neue Parameter sollen sich
  konsistent dort einreihen.
* 47 - SampleSound im Control-Panel sichtbar machen
* 50 - Anzahl der Sample-Objekte in `sample.scd` begrenzen

## 2.0 What

```gherkin
Feature: SampleSound spielt für sample.scd sinnvolle Ausschnitte

  Scenario: Standardverhalten bleibt kompatibel
    Given ein SampleSound ohne startFrac/duration
    When ich es abspiele
    Then verhält es sich wie bisher (kompletter Sample-Inhalt, kein Envelope-Cutoff)

  Scenario: Ausschnitt mit fester Dauer
    Given ein SampleSound mit startFrac = 0.1 und duration = 0.5
    When ich es abspiele
    Then beginnt die Wiedergabe bei 10% der Sample-Länge
    And endet nach 0.5s mit einem kurzen Fade-out statt einem hörbaren Klick

  Scenario: mehrere Soundobjekte mit unterschiedlich langen Samples, gleiche duration
    Given zwei SampleSounds aus unterschiedlich langen Dateien, beide mit duration = 0.4
    When ich beide abspiele
    Then klingen beide exakt 0.4s lang, unabhängig von der jeweiligen Dateilänge

  Scenario: Kurze Samples vollständig verwenden
    Given eine kurze Audiodatei
    When sample.scd daraus ein SampleSound baut
    Then wird das gesamte Sample verwendet
    And duration bleibt auf "kein Cutoff"

  Scenario: Längere Samples automatisch zuschneiden
    Given eine längere Audiodatei
    When sample.scd daraus ein SampleSound baut
    Then wird ein relevanter Ausschnitt mit hörbarem Material gewählt
    And startFrac zeigt auf diesen Ausschnitt
    And duration begrenzt die Wiedergabe auf ein einheitliches Fenster

  Scenario: Relevanz über einfache Peak-Heuristik bestimmen
    Given die Datei enthält vorne Stille oder wenig Energie
    When der Ausschnitt bestimmt wird
    Then wird nicht blind am Dateianfang gestartet
    And stattdessen ein Bereich ab dem ersten ausreichend starken Peak gewählt

  Scenario: Samples im Demo klingen ähnlich laut
    Given mehrere SampleSounds aus unterschiedlich ausgesteuerten Dateien
    When sample.scd die Sounds automatisch vorbereitet
    Then werden grosse Lautheitsunterschiede reduziert
    And kein einzelnes Sample dominiert nur wegen höherem Pegel

  Scenario: Lautheitsausgleich basiert auf dem gespielten Ausschnitt
    Given ein längeres Sample mit automatisch gewähltem Ausschnitt
    When die Lautheit für den Ausgleich bestimmt wird
    Then wird der tatsächlich verwendete Ausschnitt bewertet
    And nicht ein irrelevanter stiller Teil der Gesamtdatei

  Scenario: Dominante Spitzen werden zusätzlich begrenzt
    Given ein Sample mit hoher RMS oder harten Transienten
    When sample.scd den Lautheitsausgleich berechnet
    Then wird nicht blind bis zum RMS-Ziel verstärkt
    And ein zusätzlicher Peak-Schutz verhindert übermässig dominante Einzelsounds

  Scenario: Demo konfiguriert Samples über eine spezialisierte Klasse
    Given eine Audiodatei für sample.scd
    When das Demo ein SampleSound erstellt
    Then kommen Ausschnittswahl und Lautheitsanpassung aus SampleSoundConfigurator
    And sample.scd enthält keine direkte Heuristik-Berechnung mehr

  Scenario: SampleSoundConfigurator setzt die Demo-Heuristik konsistent um
    Given dieselbe Eingabedatei und dieselben Konfigurationswerte
    When SampleSoundConfigurator ein SampleSound vorbereitet
    Then werden startFrac, duration und amp konsistent gesetzt
    And die bestehende Demo-Heuristik bleibt fachlich erhalten
```

## 3.0 How

### 3.1 Implementation Context
* `classes/sg/sounds/SampleSound.sc`:
  - neue Instanzvariablen `startFrac` (Default 0, 0..1 — Anteil der Sample-Länge, analog zum
    bereits vorhandenen `phase`-Stil) und `duration` (Default `nil` = altes Verhalten, sonst
    Sekunden).
  - `*addSynthDef`: `PlayBuf.ar`s `startPos`-Argument = `startFrac * BufFrames.kr(buf)`. Wenn
    `duration` gesetzt ist (`durationGate`-Kontrollargument > 0 statt echtem `nil`-Check im
    SynthDef, da SynthDef-Argumente keine `nil`-Werte kennen — 0 bedeutet "kein Cutoff, altes
    Verhalten"), zusätzliches `EnvGen.kr(Env.linen(attackTime, sustainTime, releaseTime), trig,
    doneAction: 0)` als Multiplikator auf das Signal, mit kurzem Attack/Release (~5-10ms) gegen
    Klicks — sustain so berechnet, dass die Gesamtlänge exakt `duration` ergibt.
  - Instanzvariable statt Argument mit `nil`-Default: Default-Konvertierung `duration ?? 0`
    beim Synth-Start (siehe `makeSynth`), analog zum bestehenden `binauralizerClass ?? {
    Binauralizer }`-Muster in `Listener`.
  - `*editableParams` um `startFrac`/`duration` ergänzen (`ControlSpec`), damit beide auch live
    im GUI bearbeitbar sind (Konsistenz mit Intent 43).
* `tests/sg/sounds/TestSampleSound.sc`: `editableParams`-Tests um die neuen Keys erweitern
  (reine sclang-Logik, testbar wie bisher). Das eigentliche SynthDef-Verhalten (Cutoff-Envelope,
  Klickfreiheit) ist Server-seitig und wird wie bei `InsectSound`/`Binauralizer` nicht
  automatisiert getestet, sondern per Hörtest verifiziert (siehe AGENTS.md, TDD-Abschnitt).
* `demos/sample.scd`: gemeinsame `duration` für alle Soundobjekte setzen (z.B. `duration: 0.8`),
  damit der Demo-Zweck ("alle Objekte gleich lang") sichtbar wird.
* Nicht Teil dieses Intents: Zeitdehnung/-stauchung (Pitch-Shift über `rate`), Loop-Handling,
  Crossfades zwischen Retrigger-Zyklen — reines Zuschneiden auf einen festen Ausschnitt.
* Folgeausbau für `sample.scd`:
  * kurze Samples: `startFrac = 0`, `duration = 0` (ganzes File)
  * längere Samples: fester Zielausschnitt, Start über einfache Peak-Heuristik
  * vorhandene `samplePreview`-/Peak-Buckets aus `SampleSound` wiederverwenden, keine zweite
    Audioanalyse einführen
  * für den ersten Scope festgelegt:
    * `shortThreshold = 0.5` Sekunden
    * `targetDuration = 0.3` Sekunden für längere Samples
    * `peakThreshold = 0.25` relativ zu den normalisierten Peak-Buckets
    * `previewBucketCount = 96` (bestehender Default aus `samplePreview`)
  * Begründung der Schwellen auf dem aktuellen Percussion-Bestand:
    * 148 Audiodateien
    * `min = 0.287s`, `median = 1.972s`, `max = 17.006s`
    * 20 Samples `<= 0.5s`, 40 Samples `> 0.5s && <= 1.5s`, 88 Samples `> 1.5s`
    * `0.5s` trennt damit die echten kurzen One-Shots sauber von der Mehrheit der längeren
      Phrasen/Rolls
    * `0.3s` ist lang genug für einen klaren Percussion-Charakter, ohne die langen Dateien
      wieder zu stark auszudehnen
  * wenn kein Peak über Schwellwert gefunden wird: Fallback auf `startFrac = 0`
  * konkrete Regel:
    * wenn `sampleDuration <= shortThreshold`: `startFrac = 0`, `duration = 0`
    * sonst: erstes Bucket mit `peak >= peakThreshold` suchen
    * `startFrac` auf den Bucket-Anfang dieses Treffers setzen
    * `duration = targetDuration`
    * wenn kein Bucket trifft: `startFrac = 0`, `duration = targetDuration`
  * Scope bewusst nur für `demos/sample.scd`, nicht als allgemeine musikalische
    Analyse-Engine für alle Demos
* Folgeausbau für `sample.scd` Lautheitsausgleich:
  * Lautheit zunächst pragmatisch aus dem gespielten Ausschnitt schätzen, keine vollständige
    Loudness-Norm nach EBU R128 o.ä.
  * Berechnung auf Basis der bereits vorliegenden Sample-Daten in `SampleSound`, damit keine
    separate Offline-Analyse-Pipeline entsteht
  * Ausgleichsfaktor pro Sample in `demos/sample.scd` berechnen und auf sinnvolle Grenzen
    klemmen, damit leise oder fast stille Dateien nicht überverstärkt werden
  * Massstab ist "ähnlich laut im Demo", nicht perfekte wahrgenommene Lautheit in jedem
    musikalischen Kontext
  * konkrete Heuristik für den ersten Scope:
    * Messung auf dem tatsächlich gespielten Bereich (`startFrac` + `duration`, bei `duration = 0`
      auf dem gesamten Rest des Files)
    * Pegelschätzung primär über RMS des Ausschnitts, weil das die mittlere Energie besser
      abbildet als ein reiner Peak-Wert
    * zusätzlicher Peak-Wert nur als Diagnose-/Schutzinformation im Ergebnis mitführen
    * Ziel-RMS für die Demo: `0.18`
    * Gain-Faktor = `targetRms / excerptRms`, aber hart geklemmt auf `0.5 .. 2.0`
    * wenn `excerptRms` praktisch Null ist (z.B. `< 0.0001`), kein Hochziehen erzwingen,
      sondern Faktor `1.0` verwenden
    * vorhandenes `amp` von `SampleSound` bleibt die Stelle, an der der Ausgleich landet; keine
      neue Lautstärke-Property nur für diese Demo einführen
  * Nachschärfung nach Hörtest:
    * `targetRms` konservativer auf `0.12` senken
    * `maxGain` von `2.0` auf `1.4` reduzieren, damit viele Samples nicht pauschal am Anschlag
      landen
    * zusätzlicher Peak-Schutz über `maxOutputPeak = 0.6`
    * effektiver Gain = `min(rmsGain, peakSafetyGain)`, danach weiterhin auf `minGain..maxGain`
      klemmen
    * Ziel ist nicht perfekte Gleichheit, sondern dominante Samples so weit zu zähmen, dass
      feinere Klänge im Kreis-Demo hörbar bleiben
* Auslagerung in `SampleSoundConfigurator`:
  * neue Klasse unter `classes/sg/sounds/`, damit die Demo-spezifische Vorbereitung nicht mehr
    im Skript selbst lebt
  * Verantwortung der Klasse:
    * `SampleSound` aus Pfad/Rate/Phase erzeugen
    * Ausschnitt über `suggestExcerpt` wählen
    * Pegel über `excerptLevelEstimate` bewerten
    * Gain aus RMS + Peak-Schutz berechnen
    * fertige Parameter (`startFrac`, `duration`, `amp`) auf das `SampleSound` schreiben
  * die bisher in `demos/sample.scd` verstreuten Heuristik-Parameter werden als Konfiguration
    dieser Klasse gebündelt
  * `demos/sample.scd` soll danach nur noch Konfiguration anlegen, `SampleSoundConfigurator`
    aufrufen, preloaden und registrieren
  * reine sclang-Logik der neuen Klasse in eigenem UnitTest absichern; kein Server-/Audio-IO-
    Verhalten automatisiert testen, das bereits in `SampleSound` abgedeckt ist

### 3.2 Tasks
- [x] 1.0 `SampleSound` um `startFrac`/`duration` erweitern
    - [x] 1.1 TDD: `TestSampleSound.sc` — `editableParams` um `startFrac`/`duration` ergänzen,
          zuerst rot
    - [x] 1.2 `SampleSound.sc` implementieren (Konstruktor, `*addSynthDef` mit `startPos` +
          optionalem Envelope-Cutoff, `*editableParams`), headless grün
- [x] 2.0 `demos/sample.scd` nutzt eine gemeinsame `duration` für alle Soundobjekte
- [x] 3.0 Heuristik für kurze vs. lange Samples festlegen
- [x] 4.0 `SampleSound` um Hilfsmethode für automatische Ausschnittsvorschläge ergänzen
- [x] 5.0 `demos/sample.scd` auf automatische Vorschläge umstellen
- [x] 6.0 Relevante Tests ergänzen und `run_tests.scd` grün ausführen
- [x] 7.0 Lautheits-Heuristik für `sample.scd` festlegen
- [x] 8.0 `SampleSound` um Pegelschätzung des aktiven Ausschnitts ergänzen
- [x] 9.0 `demos/sample.scd` auf automatische Lautheitsanpassung umstellen
- [x] 10.0 Lautheits-Heuristik gegen dominante Samples nachschärfen
- [x] 11.0 Relevante Tests ergänzen und `run_tests.scd` grün ausführen
- [x] 12.0 `SampleSoundConfigurator` als spezialisierte Klasse einführen
- [x] 13.0 `demos/sample.scd` auf `SampleSoundConfigurator` umstellen
- [x] 14.0 Relevante Tests ergänzen und `run_tests.scd` grün ausführen
- [x] 15.0 Hörtest im Demo durch Nutzer, Intent abschliessen

## Challenges & Solutions

### Task 1.0 — Default `0` statt `nil`
Der Entwurf (3.1) ging von `duration = nil` als Default aus (mit `duration ?? 0`-Konvertierung
beim Synth-Start). Umgesetzt wurde stattdessen direkt `duration = 0` als sclang-seitiger
Default: SynthDef-Argumente kennen ohnehin kein `nil`, und `0` ist als "kein Cutoff"-Sentinel
unzweideutig — spart die zusätzliche Konvertierungsstelle, ohne die Semantik zu ändern
("ohne Angabe verhält es sich wie bisher" bleibt erfüllt). `startFrac`/`duration` sind reine
sclang-Werte (Konstruktor/Accessor), deshalb per `TestSampleSound` automatisiert abgesichert;
das eigentliche SynthDef-Verhalten (Cutoff-Envelope, Klickfreiheit) bleibt Server-seitig und
wird wie geplant nicht automatisiert getestet, nur per Sanity-Check (`SampleSound.addSynthDef`
baut ohne Fehler, headless geprüft) und späterem Hörtest (Task 3.0).
